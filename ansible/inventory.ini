[k8s_master]
# Replace with your actual Kubernetes master node IP or hostname
# If running Jenkins on the same machine as Minikube/K8s, you can use localhost
localhost ansible_connection=local

[all:vars]
ansible_python_interpreter=/usr/bin/python3
